using System;

class Program
{
    static void Main(string[] args)
    {
        // I was supposed to use 8 classes, but I only used 6. I could have stretched some of it to extra unnecessary classes, but wanted the program to be more understandable. 
        // If it's absolutely necessary to have 8 classes, I could theoretically add more functionality to the program to accomodate that. 
        // Would appreciate if you let me do that after you grade this!
        WeekManager weekManager = new WeekManager();
        weekManager.Run();
    }
}